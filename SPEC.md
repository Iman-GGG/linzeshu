# 林则鼠控烟辅助小程序 技术方案（SPEC）

版本：V1.0

目的：针对 PRD 中的功能需求，给出系统架构、关键组件、接口契约、AI 模块实现建议、数据存储与隐私策略、部署与测试计划的可执行技术方案。

---

**1. 总体架构（概要）**

- 前端：微信小程序（WeChat Mini Program）
  - 页面与组件：Home、BeforeGuide、CameraModule、Records、ContentCenter
  - 本地存储：使用微信小程序本地文件系统保存录制的完整音视频与截图路径（仅本地访问）。

- 后端（云端 API 层）：标准 REST API，部署在托管云（建议使用阿里云/腾讯云/华为云）或城市政务云。
  - 功能：接收台账元数据、内容管理、热力图聚合、用户-政府账号绑定、审计日志

- AI 服务（可托管或云端）
  - 语音识别 ASR（实时/近实时）→ 将识别结果送给对话模块
  - 轻量 NLU/规则引擎 → 匹配烟民话术并返回标准化反驳话术
  - TTS（设备端或云端）→ 播报劝阻与结束文案（优先设备端以降低延迟）

- 第三方/政府系统
  - 上海市控烟热力地图 API（对接接口协议需与政府方确认）
  - 官方举报平台（跳转 URL 或通过深度对接提交）

部署拓扑：小程序（客户端） ↔ 后端 API ↔ AI 服务（ASR/NU/TTS） ↔ 热力图/政务系统

---

**2. 关键组件设计**

2.1 客户端（小程序）

- CameraModule
  - 权限管理（摄像头、麦克风、定位）
  - 录制控制（start/stop）、本地文件保存路径管理
  - 语音播报（使用微信 TTS 或系统音频）

- RealtimeAIAdapter
  - 将音频流或短音频片段发送至 ASR 服务（优先采用短时段识别，降低延迟）
  - 接收识别文本并传入本地规则/显示模块以触发 AI 反驳话术

- ReportClient
  - 负责将台账元数据上报至云端（含重试与本地缓存机制）

2.2 后端服务

- AuthService：微信授权、会话管理、与政府账号关联逻辑
- ReportService：接收并校验台账元数据，写入 DB（MySQL/Postgres），触发热力图聚合任务
- ContentService：管理 FAQ、法规、案例，支持全文检索（Elasticsearch 或云搜索服务）
- HeatmapService：定时聚合 ReportRecord，生成热力图数据（Redis/Tile 存储或时序数据库）

2.3 AI 服务建议

- ASR：优先采用离线或近实时识别能力，若需快速上线可接入云厂商 ASR（腾讯云/科大讯飞/阿里云）
- NLU/Rules：以关键词 + 简单意图分类为主，建立反驳话术映射表（规则优先），后续可升级为小型对话模型
- TTS：可使用客户端 TTS（若微信能力允许）或云端 TTS，要求延迟 < 1s

---

**3. 数据存储与隐私策略**

3.1 存储方案

- 后端 DB：关系型数据库存储台账元数据（record_id, user_id, timestamp, lat, lng, result, summary, screenshot_url）
- 热力图：按网格/瓦片聚合存储，供地图前端拉取
- 日志：审计与错误日志写入集中日志系统（ELK 或云日志）

3.2 隐私与合规

- 媒体文件：完整音视频仅保存在用户设备；云端仅在用户授权下短期保存低分辨率截图并限制访问权限
- 定位数据：聚合后用于热力图，精确定位需严格鉴权并仅对执法人员开放
- 数据访问：采用 RBAC，政府方与平台管理方访问需双因素或行政接口鉴权

---

**4. 接口契约（示例）**

1) POST /api/v1/report
- 请求示例：

{
  "user_id": "u_123", 
  "timestamp": "2026-06-13T10:00:00Z",
  "lat": 31.2304,
  "lng": 121.4737,
  "address": "人民广场",
  "result": "failure",
  "summary": "烟民在入口处吸烟，场所无人制止",
  "screenshot_url": "https://cdn.example.com/snap/abc.jpg"
}

- 返回示例：{ "code":0, "record_id":"r_20260613_0001" }

2) GET /api/v1/heatmap?bbox=...
- 返回示例：{ "cells": [{ "cell_id":"c1","heat_value":12,"count":20,"center":{...}}] }

—— 详细接口文档在开发阶段应以 OpenAPI/Swagger 规范维护 ——

---

**5. AI 与实时性能实现建议**

- 语音识别：客户端采样 16kHz，分 3-5s 小段上传 ASR；采用流式识别以降低延迟
- 反驳逻辑：先以规则与关键词匹配实现（覆盖高频反驳场景），再逐步引入轻量检索式 + seq2seq 模型生成候选话术
- 延迟目标：ASR + NLU + TTS 总延迟 ≤ 1s（努力达到）

---

**6. 权限、错误与重试策略**

- 权限拒绝：客户端检测权限拒绝后提示并提供引导授权入口，若用户永久拒绝，隐藏相机入口并提示只能使用偷偷举报
- 上传重试：ReportClient 在网络不佳时将台账元数据缓存至本地（加密），并在网络恢复后自动重试上传
- 错误上报：关键错误上报到监控系统并触发告警（如上传失败率过高）

---

**7. 测试与上线计划**

1. 单元测试：后端服务、内容检索、接口契约
2. 集成测试：模拟上报、热力图聚合、权限边界测试
3. 性能测试：相机启动、ASR 延迟、上报 TPS
4. Beta 小范围灰度：先在部分城区或合作社区内试点

上线前应完成：安全评估、隐私合规审查、政府对接确认热力图 API 与举报跳转方案。

---

**8. 交付清单（V1.0）**

- 小程序前端（四大 Tab + 劝烟相机模块）
- 后端 API（报告、记录、内容管理、热力图聚合）
- AI 服务对接（ASR/简单 NLU/TTS）
- 安全与隐私说明文档
- 测试报告与上线运行手册

---

如需我把 SPEC.md 转为开发任务清单（JIRA/Trello 风格）或生成 OpenAPI/Mock Server，我可以继续推进。
